# Final fixed-member active-learning configuration

These files record the final fixed-member active-learning settings used for the
published comparison. The runner copies this directory into each final result
root and records file hashes in `frozen_config_hashes.json` so that the analysis
can be traced back to the exact configuration files used for the run.
