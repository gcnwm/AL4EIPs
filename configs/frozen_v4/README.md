# Frozen v4 active-learning configuration

These files freeze the primary Strategy B v4 rerun settings before corrected training.
The runner copies this directory into each `results/al_v4/{run_id}/frozen_configs/`
root and records file hashes in `frozen_config_hashes.json`.
